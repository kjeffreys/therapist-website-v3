import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <nav>
        {/* Add your navigation links here */}
      </nav>
    </header>
  );
};

export default Header;
